/**
 * Mon apprentissage GitHub Copilot - Step 2
 * Exercice: Utiliser les suggestions de code Copilot
 */

/**
 * Calculate the sum of two numbers
 * @param {number} var1 - First number
 * @param {number} var2 - Second number
 * @returns {number} The sum of var1 and var2
 */
function calculateNumbers(var1, var2) {
    return var1 + var2;
}

module.exports = { calculateNumbers };
